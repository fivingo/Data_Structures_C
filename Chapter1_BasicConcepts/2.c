// 선택 정렬 알고리즘

#include <stdio.h>

void main() {
    for (i = 0; i < n; i++) {
        // list[i] 에서 부터 list[n-1] 까지의 정수 값을 검사한 결과;
        // list[min] 이 가장 작은 정수 값이라 하자;

        // list[i] 와 list[min]을 서로 교환;
    }
}